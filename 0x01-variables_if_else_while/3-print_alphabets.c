#include <stdio.h>

/**
 * main - Entry point of the program 
 *
 * Return: 0 on success
 */
int main(void)
{
	char letter;

	for (letter = 'a' 'A'; letter <= 'z' 'Z'; letter++)
		putchar(letter);

	putchar('\n');
	return (0);
}
